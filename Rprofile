chooseCRANmirror(graphics=FALSE)

if (interactive()) {
  # Default repo
  local({r <- getOption("repos")
         r["CRAN"] <- "http://cran.stat.sfu.ca"
         options(repos=r)
  })

	library("setwidth")
	library("colorout")
}
